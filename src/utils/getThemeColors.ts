import { theme } from '@chakra-ui/react';

// const getColorCode = (themeColor: string): string => {
//     const [palette, color]:  = themeColor.split('.');

//     return theme.colors[palette][color];
// }
